# 07_auto-learning-bot Agent 開發規範

本專案遵循目前有效之全域開發憲法；本檔僅定義專案專屬規則與例外。

---

## 1. 技術棧與環境邊界
- **主力架構**：Python 3.13 + Selenium + ChromeDriver + Tkinter。
- **自動化引擎邊界**：
  - 目前 Selenium 流程針對公務平台（臺北 E 大、e 等公務園）之 Session 維護、動態 DOM 與 Alert 彈窗攔截已經過長期穩定實戰檢驗，**嚴禁僅因 Playwright 較新穎而隨意更換底層自動化引擎**。
  - 主程式漸大時應優先進行模組化重構、抽離業務模型與補強單元測試，不因規模擴增更換語言。
- **可攜發布模式**：
  - 維持免安裝綠色版打包架構（基於 `python_embed`），啟動腳本遵從全域 BAT 薄啟動器原則，核心環境檢查與啟動置於 PowerShell (`setup_and_run.ps1`)。

---

## 2. 業務領域與架構隔離邊界
- **雙平臺邏輯嚴格分離**：
  - 「臺北 E 大」與「e 等公務園+學習平臺」之課程導航、及格標準（動態及格門檻判定）、認證按鈕識別與問卷結構各異，邏輯必須獨立模組化隔離，不得相互耦合或因單一平臺改版而破壞另一平臺運作。
- **以「課程狀態模型」為中心**：
  - 工作台應以「課程狀態」（課程名稱、當前進度、累計時數、測驗/問卷狀態、失敗原因、下一步動作）為核心資訊架構。
  - 除錯用日誌（Console / Debug Log）應歸屬於第二層檢視視窗，嚴禁將大量原始 Log 作為唯一的使用者回饋介面。
- **作答與題庫邊界**：
  - 本機持久化題庫應優先於外部 AI 查詢，以降低操作延遲、外部網路依賴與 API 使用量。
  - 外部 AI 輔助作答必須維持結構化輸出驗證、安全滑動限速（防範服務端限流阻斷）與 API Key 本機脫敏遮罩（絕不上傳外洩）。

---

## 3. 穩定性、安全與防禦規範
- **平台死循環阻斷**：
  - 遇到 `ERR_TOO_MANY_REDIRECTS`、非上課期間 Alert、課程尚未上架彈窗或無權限課程，必須主動標記、安全略過（Catch and Skip）並計入待人工查核清單，嚴禁觸發無限重複登入。
  - 定期 Session 保養（定時清理 Cookie 並重新取得 SSO 憑證）必須於單門課程完整結算後安全執行，不中途破壞測驗或上課狀態。
- **敏感資訊防線**：
  - 使用者公務帳號密碼與 Gemini API Key 僅能存放於本機 `data/config.json`，在日誌、錯誤回報與介面顯示中一律強制脫敏（Masking）。

---

## 4. 驗證方式
- 修改課程邏輯、AI 作答或安全性模組後，必須執行自動化單元測試：
  ```bash
  pytest tests/ -q
  ```
