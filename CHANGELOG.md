# 變更紀錄

## [未發布]

## [V3.2.0] - 2026-09-01

### 文件

- 明列 Python 3.13／PySide6／Selenium 為主力架構；本次未進行語言遷移。

### 新增與修正

- **研習成效與配額追蹤**：新增每門課程結果卡片、完成／及格計數與每日 AI 用量的本機持久化紀錄，讓執行結果與剩餘額度可追溯。
- **AI 答案品質防護**：僅將取得滿分的 AI 作答結果寫入本機題庫，避免未達標答案污染後續比對資料。
- **批次測驗請求收斂**：單一測驗改為一次批次 AI 請求；批次失敗時交由既有人機協同流程處理，不再逐題重複請求。
- **資訊揭露校正**：AI 服務方案與模型說明改為引導使用者查閱官方即時公告，移除易過期的固定資費敘述。

### 維護

- `run.bat` 優先使用 PowerShell 7；未安裝時自動退回 Windows PowerShell 5.1，維持既有自癒啟動流程。

本專案依循 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/) 的格式記錄重要異動。

## [V3.1.1] - 2026-08-27

### 🏆 核心特色

- **動態及格門檻多重判定機制**：
  - 支援 60、70、75、80、100 分等多種平臺及格標準，精準對齊專題與開放式課程（如「臺灣藍碳發展機會與策略建議」之 75 分門檻）。
  - 將平臺 `pass_status`（未通過/不及格/fail）納入最高優先排外過濾，即使考取 60 分亦絕不誤判為修畢，確保自動進入考試流程補測達標。
- **AI 測驗助理多選項（E、F...）與數字代號解析升級**：
  - 答案解析引擎（`parse_ai_quiz_answers`）放寬正則限制，完整支援 5 選項題型（如「英業達實務案例」等題型包含 `E. 以上皆是`）。
  - 支援 1～9 數字代號自動對應至第 N 個選項代碼（如 `5` 自動對應為 `E`），提升人機協同回貼之容錯率。

### 修正

- **非上課期間與尚未上架課程 Alert 攔截**：
  - 自動捕捉並確認「目前課程尚未上架且非上課期間，無法進入教室介面」等彈窗，主動標記並永久跳過，阻斷重登重試之無限循環。
- **加盟機關導航與非教室視窗排除**：
  - 教室尋找機制增強，自動排除加盟機關入口首頁（`mooc/index.php`）與學員統計頁（`learn_stat.php`），避免在未開啟實際教材時空轉。
- **開放式課程「認證」按鈕精確識別**：
  - 在未完成課程清單與自動測驗流程中，擴充支援【認證】、【進行測驗】等多元按鈕標籤點擊。

## [V3.1.0] - 2026-08-25

### 🏆 核心特色

- **主動定期 Session 保養與 Cookie 深度清理重登機制**：
  - 研習主迴圈新增自動定時維護機制，預設每連續研習滿 **5 小時**（可於 `config.json` 的 `session_refresh_hours` 彈性調整），於課程完成結算後自動啟動。
  - 保養流程主動關閉殘留子分頁，呼叫 `delete_all_cookies()` 與 `http_session.cookies.clear()` 深度清除積累之快取與過期 Session，並重新執行我的 E 政府完整登入與 `sync_session()`，獲取 100% 全新 SSO 憑證。
  - 機制全面覆蓋「單門逐面上課流程」與「批次問卷/測驗結算流程」，確保長時間掛機（8~24 小時以上）穩定不中斷。

### 修正

- **平臺重新導向異常 (ERR_TOO_MANY_REDIRECTS) 自動跳過**：
  - 自動偵測 Chrome `ERR_TOO_MANY_REDIRECTS`（重新導向次數過多）與 `ERR_NAME_NOT_RESOLVED` 等平臺端故障，安全跳過（SKIP）該課程並記錄至待人工查核清單，避免程序卡死。
- **登出狀態精確判定與死循環阻斷**：
  - 排除 `mooc/index.php` 正常課程路徑之登出誤判，登出偵測改採精準關鍵字。
  - 新增單一課程重登重試防護鎖（Relogin Loop Guard），連續觸發 2 次異常自動略過，徹底消除無限重新登入死循環。

## [V3.0.1] - 2026-08-24

### 修正

- **背景模式切頁偶發跳出 Chrome**：
  - Win32 視窗隱藏新增約 2 秒的短期防護，持續攔截 Selenium 點擊後延遲建立的 Chrome HWND。
  - 同一個 WebDriver 重複觸發只延長防護期限，避免建立多個重複背景執行緒。
  - 補齊課程、測驗、問卷與臺北 E 大 SCORM 新視窗切換後的隱藏處理。
  - 使用者主動顯示瀏覽器時立即取消尚未到期的隱藏防護，避免視窗再次被隱藏。
- **Windows PowerShell 5.1 更新器相容性**：
  - 避免 `Path`／`PATH` 環境變數大小寫重複造成 `Start-Process` 失敗，改以不重建環境字典的方式啟動新版與還原舊版。
- **GitHub Release 資產名稱相容性**：
  - 可攜版 ZIP 改採穩定 ASCII 資產名稱 `AdminEfficiencyPilot_V3.0.1_Portable.zip`，避免 GitHub 移除中文檔名後導致更新器無法精確比對。

### 驗證

- 新增視窗隱藏防護單元測試，涵蓋延遲重試、重複請求去重與顯示操作取消防護。
- Windows PowerShell 5.1 更新成功切換與啟動失敗 rollback 模擬均通過。

## [V3.0.0] - 2026-08-24

### 🏆 核心特色

- **Python 3.13 現代架構統一**：
  - 開發、測試與可攜版封裝管線全面統一以 Python 3.13 64-bit 為基準，全面提升套件相容性與執行效能。
- **人機協同測驗助理全面升級 (Interactive Quiz Assistant v2)**：
  - 題目複製區提供「🌐 開啟 ChatGPT」與「✨ 開啟 Gemini」捷徑按鈕，以系統原生機制開啟預設瀏覽器。
  - 回貼區全面升級純文字貼上與多格式容錯正則，支援 Markdown 粗體、清單符號、表格列與常見中英文引導詞。
  - 是非題改採**否定詞優先比對**，徹底修復「不正確」、「不是」被肯定詞誤判之問題。
  - 支援無題號之**純行答案解析**（如 `A\nD\nC\nB`），於行數符合時自動依題序映射。
  - **題庫寫入防護**：人機協同回貼答案只用於當次作答，不寫回 SQLite 題庫，防止錯誤答案、題序變動或題組抽換污染題庫。
  - 實作跨平臺全域互斥鎖（Anti-Crash Queue Lock），雙開同時遇到測驗時自動排隊依序彈窗，避免 Qt 模態事件循環衝突。
- **磨課師 (MOOCs) 播放器相容與外購平臺略過機制**：
  - 支援 e 等公務園現代磨課師與影音播放器，自動跨 iframe 執行靜音播放與心跳維護。
  - 外購第三方平臺（如 Hahow）遇轉址或提示時，自動略過（SKIP）並優先研習本平臺原生課程。
  - 偵測到 Hahow 裝置上限頁面時提供處理機制，避免程序停滯。
- **Win32 原生背景隱藏模式與彈出視窗管理**：
  - 支援「👁️ 顯示瀏覽器 / 🙈 隱藏瀏覽器」隨時切換，以 Windows 原生 API 控制 Chrome 視窗顯示狀態。
  - 以 `CREATE_NO_WINDOW` 降低命令列視窗出現機率，並在課程與測驗開窗節點嘗試管理新視窗顯示狀態。
- **系統安全性與可靠度強化**：
  - 設定檔改採原子寫入機制（`write_json_atomically`），防範斷電或當機時 `config.json` 毀損。
  - 更新下載流程整合 SHA-256 完整性雜湊驗證，雜湊不符時自動中止更新並刪除損壞檔案。
  - 可攜版改採 `current/` 程式與 `data/` 個人資料分流；更新不覆蓋帳密、題庫與日誌。
  - GitHub Release 檢查支援 ETag、Markdown 發布說明、精確資產名稱與 asset digest；缺少 digest 時禁止自動安裝。
  - Portable ZIP 更新新增安全 staging、Zip Slip／symbolic link 防護、內部雜湊清單驗證、同磁碟切換與啟動失敗 rollback。
  - 可攜版封裝由 `version.txt` 取得版本，乾淨重建 Python 3.13 `site-packages`，並產生 `SHA256SUMS.txt` 與 ZIP 雜湊檔。

### 修正

- **課程完成判定與任務狀態修正**：
  - 移除待上課清單過濾條件中對測驗/問卷的錯誤排除，時數不足之課程一律保留進行研習。
  - 問卷填寫成功才標記為已完成；失敗則記入人工處理。
  - 測驗連續 3 次不及格時精確記入待人工處理清單，結束時明確提示，不再錯誤宣稱「所有任務圓滿達成」。
- **組裝課程處理流程優化**：
  - e 等公務園報名組裝課程後，平臺會自動加入子課程；本程式直接依 API 課程清單處理，避免重複掃描與重複報名。子課程缺漏時才使用修復備援。

## [V2.5.0] - 2026-08-24

### 🏆 核心特色

- **磨課師 (MOOCs) 播放器相容與外購平臺略過機制**：
  - 支援 e 等公務園現代磨課師與影音播放器，自動跨 iframe 執行靜音播放與心跳維護。
  - 外購第三方平臺（如 Hahow）遇轉址或提示時，自動略過（SKIP）並優先研習本平臺原生課程。
  - 偵測到 Hahow 裝置上限頁面時提供處理機制，避免程序停滯。
- **Win32 原生背景隱藏模式與彈出視窗管理**：
  - 支援「👁️ 顯示瀏覽器 / 🙈 隱藏瀏覽器」切換，以 Windows 原生 API 控制 Chrome 視窗顯示狀態。
  - 以 `CREATE_NO_WINDOW` 降低命令列視窗出現機率，並在課程與測驗開窗節點嘗試管理新視窗顯示狀態。
- **人機協同測驗助理全面升級 (Interactive Quiz Assistant v2)**：
  - 題目複製區新增「🌐 開啟 ChatGPT」與「✨ 開啟 Gemini」捷徑按鈕，以 Windows 原生機制開啟預設瀏覽器。
  - 回貼區全面升級純文字貼上與多格式容錯正則，支援 Markdown 粗體（`**1. B**`）、清單符號（`- 1. B`）、表格列（`| 1 | B |`）與常見中英文引導詞。
  - 新增即時解析回饋標籤（Live Parsing Feedback），貼上時即時顯示已識別之題號與答案清單。
- **全域人機作答排隊互斥鎖（Anti-Crash Queue Lock）**：
  - 實作跨平臺全域互斥鎖，雙開同時遇到測驗時自動排隊依序彈窗，避免 Qt 模態事件循環衝突。

### 修正

- **重構課程完成與問卷防呆判定**：
  - 依個別課程實際要求確認研習時數、測驗及格（若有測驗）與問卷完成（若有問卷）；避免測驗 0 分但問卷已填的課程被誤判完成。
  - 問卷已完成之課程自動跳過填寫，防止重複送出問卷。
- **組裝課程處理流程優化**：
  - e 等公務園報名組裝課程後，平臺會自動加入子課程；本程式直接依 API 課程清單處理，避免重複掃描與重複報名。子課程缺漏時才使用修復備援。

## [V2.4.0] - 2026-08-17

### 新增與修正

- 新增 Python 3.13 `RUN.bat` 自癒啟動與下載後安裝說明。
- 修正啟動器入口判斷，桌面模式會優先啟動 `ui.py`，不再誤開命令列核心。
- 明確區分 Python 3.13 開發環境與 Python 3.11 可攜版封裝依賴。

## [V2.3.0] - 2026-08-16

### 新增

- **人機協同測驗助理（免 API Key 模式）**：
  - 遇到測驗時自動彈窗，支援「📋 一鍵複製 AI 提問詞」。
  - 使用者在 ChatGPT 或 Gemini 網頁版問出答案後，可直接回貼（支援剪貼簿一鍵貼上）。
  - 智慧解析單選、是非（⭕/❌/是/否）與多選題，自動精確點選網頁核取方塊並交卷。
  - 內建 180 秒倒數計時（分秒動態顯示 03:00）、暫停倒數與逾時明確決策提示（重設計時／跳過／結束執行），防範掛網無人操作時卡死。
  - 作答完畢後自動將題目與解答沉澱至本機 SQLite 題庫 `questions.db`。（V3.0.0 起，人機協同答案不再寫入。）
- **組裝／套裝課程自動展開與多頁選課**：
  - 支援 e 等公務園「組裝課程」與「套裝課程」，自動切換至「課程資訊」頁籤萃取所有子課程清單。
  - 排除側邊欄干擾，逐一自動進入子課程完成報名與選課。
  - 支援跨 1~30 頁以及學習儀表板多頁面組裝課程完整掃描。
- **測驗即時成績判定與分數通知**：
  - 測驗交卷後即時解析分數與狀態，於日誌明確顯示 `🎉 測驗結果：及格 / 通過 【得分：XX 分】` 或 `❌ 測驗結果：不及格 / 未通過 【得分：XX 分】`。

### 修正

- **測驗未及格重考修復**：修正先前已考過但不及格（如 30分、50分）時因分數非空被誤判完成的問題，精確納入待重測隊列。
- **防止誤判課程下架**：移除全文 `page_source` 字串比對，改為精準判定系統彈窗，避免無關文字誤觸略過。

## [V2.2.1] - 2026-08-13

### 新增

- 新增「本次跳過測驗，先做問卷」選項，僅套用於當次執行。

### 修正

- 僅在課程時數達標後，才允許略過測驗並嘗試填寫問卷；時數不足時會跳過該課程的測驗與問卷流程。
- 臺北 E 大執行鎖改以 PID 是否仍存活判斷，避免長時間執行超過六小時而被誤判為過期。
- 更新檢查來源改為目前專案的 `origin` 儲存庫。
- 補齊 SQLite 連線工具的必要匯入。

### 文件

- 以 UTF-8 重寫 README 與本檔案，並統一版本資訊與實際自動化工具描述。

## [V2.2.0] - 2026-08-01

### 新增

- 導入 SQLite 本機題庫快取與題目比對流程。
